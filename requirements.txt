# ─────────────────────────────────────────────────────────────
#  BharatiyaSeva – Python Dependencies
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9         # file uploads

# Config
pydantic==2.7.1
pydantic-settings==2.2.1

# MongoDB async driver
motor==3.4.0
pymongo==4.7.2

# PDF parsing
PyMuPDF==1.24.3                 # fitz

# LangChain (chunking + future RAG)
langchain==0.2.1
langchain-community==0.2.1

# Embeddings (free, local)
sentence-transformers==3.0.1
torch==2.3.0                    # CPU-only is fine; add --index-url for CUDA

# Async utilities
asyncio-throttle==1.0.2

# Logging
python-json-logger==2.0.7

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                   # async test client for FastAPI

# Future – install when needed
# redis==5.0.4
# flashrank==0.0.10
# ragas==0.1.10
# langsmith==0.1.63

langchain-huggingface==0.0.3
